import  './style.css'

export const CommonPool = ({children}) => {
     return (
          <div className="common-pool">
               { children}
          </div>
     )
}

export default CommonPool